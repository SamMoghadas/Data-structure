public class Node {     // Nodes have 2 part   :
    int data ;          // first is data, the main thing
    Node next ;         // second is the pointer

    public Node (int data) {
        this.data = data ;
        this.next = null ;
    }
}
